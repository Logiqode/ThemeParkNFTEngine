FROM golang:1.26-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /bin/minter ./cmd/minter

FROM gcr.io/distroless/static-debian12
COPY --from=builder /bin/minter /bin/minter
EXPOSE 8080
ENTRYPOINT ["/bin/minter"]
